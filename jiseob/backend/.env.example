# Copy ../../.env.example to ../../.env for local development.
# NestJS can load the repo-root .env when ConfigModule is wired.
AGENT_MODEL=gpt-4.1-mini
AGENT_TIMEOUT_MS=30000
AGENT_MAX_OUTPUT_TOKENS=1200
